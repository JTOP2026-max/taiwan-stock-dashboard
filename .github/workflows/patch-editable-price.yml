name: Legacy paused - Patch editable price

on:
  workflow_dispatch:

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow paused. V5 production automation does not use this workflow."
